# sim2cpp

This repository collects some examples of default Simulink models
converted into C++ code.
Use `make clean`, `make` and `make run` to test them.
They have been tested with Ubuntu 24.04 LTS.
